function Home({ description }) {
    return (
        <div>
            <h1>Página Inicial</h1>
            <p style={{ fontSize: "20px", textAlign: "center" }}>{description}</p>
        </div>
    )
}

export default Home;